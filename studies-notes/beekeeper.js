/* ------------------- Beekeeper -----------------------------------------------------------------------------

Menu "New Connection" --- escolhe o tipo de conexão (ex: MySQL - SQLite - SQL Server - etc)

  "Choose File" --- onde está o arq. "database" (escolher e depois clicar em "Connect")


"Entities" --- na aba da esq; são as tabela de dados
               Tabelas --- devem ter nomes escritos em minúsculo; e palavras-reservadas em MAIÚSCULO
  
  campo da direita --- onde se vai codar a tabela de SQL

  botão "run" --- fica no meio à direita; ao terminar de codar, clicar em "run" pra criar a tabela
              --- *** se selecionar (grifar parte do texto), só a parte grifada vai ser executada pelo "run"


*** Sintaxe
        --- na escrita em SQL, não pode usar espaços na escrita de um item (dentro do item); em vez disso, pôr underscore

        --- ";" é obrigatório pra separar blocos de código em SQL, estilo no CSS
            ao fazer isso, o código é executado bloco por bloco, começando de onde você deixou o cursor

*/
